#include <bits/stdc++.h>
using namespace std;
#define L long long
#ifdef LOCAL
#define dbg(x) cerr << '[' << (#x) << " = " << (x) << ']' << endl;
#else
#define dbg(x)
#endif
// Compile: g++ -std=c++17 -O2 -DLOCAL -o a a.cpp
// Runtime error catching: -D_GLIBCXX_DEBUG -D_GLIBCXX_DEBUG_PEDANTIC -fsanitize=address -fsanitize=undefined -fno-sanitize-recover

int main() {
  cin.tie(0)->sync_with_stdio(0);
}
